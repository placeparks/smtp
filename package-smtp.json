{
  "name": "smtp-server-standalone",
  "version": "1.0.0",
  "description": "Standalone SMTP server for receiving emails",
  "main": "smtp-server-standalone.js",
  "scripts": {
    "start": "node smtp-server-standalone.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongoose": "^7.0.0",
    "smtp-server": "^3.11.0",
    "mailparser": "^3.6.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

